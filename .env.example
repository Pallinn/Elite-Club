DATABASE_URL="postgresql://user:password@localhost:5432/no_signal?schema=public"

AUTH_SECRET=""
APP_URL="http://localhost:3000"

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# PromptPay ID to receive payments on (phone number, national ID, or tax ID,
# digits only e.g. "0812345678")
PROMPTPAY_ID=""

# Account holder name registered to PROMPTPAY_ID, exactly as it appears on
# bank slips (e.g. "SOMCHAI JAIDEE"). Used to reject slips paid to a
# different account than ours, even when the amount matches.
PROMPTPAY_RECEIVER_NAME=""

# Slip2Go (https://app.slip2go.com) - slip verification API
SLIP2GO_API_KEY=""
# Origin ONLY, no path (src/lib/slip2go.ts appends /api/verify-slip/... itself)
SLIP2GO_API_BASE_URL="https://connect.slip2go.com"

RESEND_API_KEY=""
RESEND_FROM_EMAIL="No Signal <tickets@no-signal.example>"

CRON_SECRET=""
